import React from 'react';
import ReactDOM from 'react-dom';

/*Everything we create in react will be inside the App component
which we then put to the root*/
import App from './App';

ReactDOM.render(<App />, document.querySelector('#root'));
